---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: applications.platform.steward.sh
spec:
  group: platform.steward.sh
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.team
      name: Team
      type: string
    - jsonPath: .spec.tier
      name: Tier
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Application is the Schema for the applications API.
          It represents a complete application with its workload and infrastructure.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the Application.
            properties:
              cache:
                description: Cache configures a managed cache (Redis/Memcached) for
                  this application.
                properties:
                  highAvailability:
                    default: false
                    description: HighAvailability enables replication.
                    type: boolean
                  size:
                    default: small
                    description: Size is the resource size abstraction.
                    enum:
                    - small
                    - medium
                    - large
                    - xlarge
                    type: string
                  type:
                    description: Type is the cache engine (redis, memcached).
                    enum:
                    - redis
                    - memcached
                    type: string
                required:
                - type
                type: object
              database:
                description: Database configures a managed database for this application.
                properties:
                  backup:
                    description: Backup configures automated backup settings.
                    properties:
                      enabled:
                        default: true
                        description: Enabled turns on automated backups.
                        type: boolean
                      retentionDays:
                        default: 7
                        description: RetentionDays is how long to keep backups.
                        format: int32
                        maximum: 35
                        minimum: 1
                        type: integer
                      window:
                        description: Window is the preferred backup window (e.g.,
                          "03:00-04:00").
                        pattern: ^([01]?[0-9]|2[0-3]):[0-5][0-9]-([01]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                    type: object
                  highAvailability:
                    default: false
                    description: |-
                      HighAvailability enables multi-AZ/replica deployment.
                      Critical tier applications should always enable this.
                    type: boolean
                  size:
                    default: small
                    description: |-
                      Size is the resource size abstraction.
                      The platform maps this to provider-specific instance types.
                    enum:
                    - small
                    - medium
                    - large
                    - xlarge
                    type: string
                  type:
                    description: Type is the database engine (postgres, mysql).
                    enum:
                    - postgres
                    - mysql
                    type: string
                  version:
                    description: |-
                      Version is the major version (e.g., "15" for PostgreSQL 15).
                      Minor/patch versions are managed by the platform.
                    pattern: ^[0-9]+$
                    type: string
                required:
                - type
                - version
                type: object
              dependencies:
                description: |-
                  Dependencies lists other Applications this service depends on.
                  Used for service discovery, deployment ordering, and impact analysis.
                items:
                  description: DependencySpec defines a service dependency.
                  properties:
                    name:
                      description: Name is the name of the dependent Application.
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the dependent Application.
                        Defaults to the same namespace as this Application.
                      type: string
                    required:
                      default: false
                      description: |-
                        Required indicates if this dependency must be healthy.
                        If true, the application won't start if dependency is down.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
              observability:
                description: Observability configures metrics, tracing, and logging.
                properties:
                  logging:
                    description: Logging configures log collection.
                    properties:
                      format:
                        default: json
                        description: Format is the log output format.
                        enum:
                        - json
                        - logfmt
                        - text
                        type: string
                    type: object
                  metrics:
                    description: Metrics configures Prometheus metrics scraping.
                    properties:
                      enabled:
                        default: true
                        description: Enabled turns on metrics collection.
                        type: boolean
                      path:
                        default: /metrics
                        description: Path is the metrics endpoint (e.g., "/metrics").
                        type: string
                      port:
                        default: 9090
                        description: Port is the metrics port.
                        format: int32
                        type: integer
                    type: object
                  tracing:
                    description: Tracing configures distributed tracing.
                    properties:
                      enabled:
                        default: false
                        description: Enabled turns on distributed tracing.
                        type: boolean
                      sampleRate:
                        anyOf:
                        - type: integer
                        - type: string
                        default: "0.1"
                        description: SampleRate is the percentage of requests to trace
                          (0.0-1.0).
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
              owner:
                description: |-
                  Owner is the email or identifier of the primary owner.
                  This person receives critical alerts and is the escalation point.
                minLength: 1
                type: string
              queue:
                description: Queue configures a managed message queue for this application.
                properties:
                  deadLetterQueue:
                    description: DeadLetterQueue configures failed message handling.
                    properties:
                      enabled:
                        default: true
                        description: Enabled turns on dead letter queue.
                        type: boolean
                      maxReceiveCount:
                        default: 5
                        description: MaxReceiveCount is how many times to retry before
                          DLQ.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                    type: object
                  fifo:
                    default: false
                    description: |-
                      FIFO enables exactly-once, ordered delivery.
                      Only supported by some queue types (SQS FIFO, Kafka).
                    type: boolean
                  type:
                    description: Type is the queue system (sqs, rabbitmq, kafka).
                    enum:
                    - sqs
                    - rabbitmq
                    - kafka
                    type: string
                required:
                - type
                type: object
              scaling:
                description: Scaling defines horizontal pod autoscaling configuration.
                properties:
                  maxReplicas:
                    description: MaxReplicas is the maximum number of replicas.
                    format: int32
                    minimum: 1
                    type: integer
                  metrics:
                    description: Metrics defines the scaling metrics to use.
                    items:
                      description: ScalingMetric defines a metric for autoscaling.
                      properties:
                        target:
                          description: Target is the target value (percentage for
                            cpu/memory).
                          format: int32
                          maximum: 100
                          minimum: 1
                          type: integer
                        type:
                          description: Type is the metric type (cpu, memory, custom).
                          enum:
                          - cpu
                          - memory
                          - custom
                          type: string
                      required:
                      - target
                      - type
                      type: object
                    type: array
                  minReplicas:
                    default: 1
                    description: MinReplicas is the minimum number of replicas.
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - maxReplicas
                type: object
              storage:
                description: Storage configures object storage (S3/GCS) for this application.
                properties:
                  encryption:
                    default: true
                    description: Encryption enables server-side encryption.
                    type: boolean
                  type:
                    description: Type is the storage system (s3, gcs).
                    enum:
                    - s3
                    - gcs
                    type: string
                  versioning:
                    default: false
                    description: Versioning enables object versioning.
                    type: boolean
                required:
                - type
                type: object
              team:
                description: |-
                  Team that owns this application. Used for access control,
                  cost allocation, and service discovery.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              tier:
                default: standard
                description: |-
                  Tier indicates the service level for this application.
                  Affects SLAs, resource allocation, and monitoring thresholds.
                    - critical: 99.99% uptime SLA, highest priority
                    - standard: 99.9% uptime SLA, normal priority
                    - development: No SLA, can be disrupted during off-hours
                enum:
                - critical
                - standard
                - development
                type: string
              workload:
                description: |-
                  Workload defines the container workload to deploy.
                  If not specified, only infrastructure is provisioned.
                properties:
                  args:
                    description: Args provides arguments to the command.
                    items:
                      type: string
                    type: array
                  command:
                    description: Command overrides the container entrypoint.
                    items:
                      type: string
                    type: array
                  env:
                    description: |-
                      Env defines environment variables for the container.
                      If InjectCredentials is true (default), infrastructure credentials are
                      appended automatically. User-defined vars take precedence over injected ones.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  envFrom:
                    description: |-
                      EnvFrom allows bulk-injecting environment variables from Secrets or ConfigMaps.
                      Use this for external credentials (Stripe keys, Google API keys, etc.)
                      that are managed outside the platform.

                      The referenced Secrets/ConfigMaps must already exist in the same namespace.
                      They can be created manually, via CI/CD, Sealed Secrets, or
                      External Secrets Operator.

                      HOW IT WORKS:
                        Every key in the referenced Secret becomes an env var in the container.
                        For example, a Secret with data {"STRIPE_KEY": "sk_live_..."} injects
                        STRIPE_KEY=sk_live_... into the pod.

                      COMPARISON WITH Env:
                        ┌──────────────────────────────────────────────────────────────────────┐
                        │ Field     │ Use Case                    │ Granularity               │
                        ├───────────┼─────────────────────────────┼───────────────────────────┤
                        │ env       │ Individual vars or overrides │ One var at a time         │
                        │ envFrom   │ Bulk-mount external Secrets  │ Entire Secret/ConfigMap   │
                        └───────────┴─────────────────────────────┴───────────────────────────┘
                    items:
                      description: EnvFromSource represents the source of a set of
                        ConfigMaps or Secrets
                      properties:
                        configMapRef:
                          description: The ConfigMap to select from
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                        prefix:
                          description: |-
                            Optional text to prepend to the name of each environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        secretRef:
                          description: The Secret to select from
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    type: array
                  healthCheck:
                    description: HealthCheck configures liveness and readiness probes.
                    properties:
                      failureThreshold:
                        default: 3
                        description: FailureThreshold is consecutive failures to mark
                          unhealthy.
                        format: int32
                        type: integer
                      initialDelaySeconds:
                        default: 10
                        description: InitialDelaySeconds is seconds before first probe.
                        format: int32
                        type: integer
                      path:
                        description: Path is the HTTP path for health checks (e.g.,
                          "/health").
                        pattern: ^/.*$
                        type: string
                      periodSeconds:
                        default: 10
                        description: PeriodSeconds is how often to probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to check.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - path
                    - port
                    type: object
                  image:
                    description: |-
                      Image is the container image to run.
                      Examples: ghcr.io/company/app:v1.0.0, nginx:latest
                    minLength: 1
                    type: string
                  injectCredentials:
                    default: true
                    description: |-
                      InjectCredentials controls whether the platform automatically injects
                      environment variables for provisioned infrastructure (database, cache, queue).

                      When true (the default), well-known env vars are injected into the container
                      from the auto-created credential Secrets:
                        - Database (postgres): DATABASE_URL, PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE
                        - Database (mysql):    DATABASE_URL, MYSQL_HOST, MYSQL_PORT, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE
                        - Cache (redis):       REDIS_URL, REDIS_HOST, REDIS_PORT, REDIS_PASSWORD
                        - Queue (rabbitmq):    AMQP_URL, RABBITMQ_HOST, RABBITMQ_PORT, RABBITMQ_USER, RABBITMQ_PASSWORD

                      Set to false only if you need full control over how credentials reach your app
                      (e.g., a sidecar reads credentials from a file instead of env vars).

                      WHY AUTO-INJECT:
                        ┌──────────────────────────────────────────────────────────────────────┐
                        │ Platform        │ Credential Strategy                               │
                        ├─────────────────┼───────────────────────────────────────────────────┤
                        │ Heroku          │ Auto-inject DATABASE_URL into every dyno          │
                        │ Railway         │ Auto-inject ${{Postgres.DATABASE_URL}}            │
                        │ Render          │ Auto-inject connection strings for linked DBs     │
                        │ Steward         │ Auto-inject well-known env vars (this feature)    │
                        │ Crossplane      │ Manual: user wires Secrets via compositionRef     │
                        └─────────────────┴───────────────────────────────────────────────────┘
                    type: boolean
                  ports:
                    description: Ports defines the ports the container exposes.
                    items:
                      description: ContainerPort defines a port exposed by the container.
                      properties:
                        containerPort:
                          description: ContainerPort is the port number on the container.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        name:
                          description: Name is the port name (e.g., "http", "grpc",
                            "metrics").
                          type: string
                        protocol:
                          default: TCP
                          description: Protocol is the network protocol (TCP/UDP).
                          type: string
                      required:
                      - containerPort
                      - name
                      type: object
                    type: array
                  replicas:
                    default: 1
                    description: |-
                      Replicas is the desired number of pod replicas.
                      When spec.scaling is set (or any other autoscaler targets the Deployment)
                      this is only the count the Deployment is created with, clamped into the
                      autoscaling range: the autoscaler owns the replica count from then on and
                      editing this field no longer moves it.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources defines CPU and memory requests/limits.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                required:
                - image
                type: object
            required:
            - owner
            - team
            type: object
          status:
            description: Status defines the observed state of the Application.
            properties:
              conditions:
                description: |-
                  Conditions provide detailed status information.
                  Standard K8s conditions pattern - each condition type has status True/False/Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              estimatedMonthlyCost:
                description: |-
                  EstimatedMonthlyCost shows the estimated monthly cost.
                  Updated during provisioning based on selected resources.
                properties:
                  amount:
                    description: Amount is the total estimated monthly cost.
                    type: string
                  breakdown:
                    additionalProperties:
                      type: string
                    description: Breakdown shows per-resource costs.
                    type: object
                  currency:
                    default: USD
                    description: Currency is the currency code (USD, EUR, etc.).
                    type: string
                type: object
              infrastructure:
                description: |-
                  Infrastructure contains endpoints and references for provisioned resources.
                  Populated after successful provisioning.
                properties:
                  cache:
                    description: Cache contains cache connection information.
                    properties:
                      endpoint:
                        description: Endpoint is the cache host.
                        type: string
                      port:
                        description: Port is the cache port.
                        format: int32
                        type: integer
                    type: object
                  database:
                    description: Database contains database connection information.
                    properties:
                      endpoint:
                        description: Endpoint is the database host (e.g., mydb.xxx.rds.amazonaws.com).
                        type: string
                      port:
                        description: Port is the database port.
                        format: int32
                        type: integer
                      secretRef:
                        description: |-
                          SecretRef references the Secret containing credentials.
                          Secret contains: username, password, database, host, port, and connection_url.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  queue:
                    description: Queue contains queue URLs and ARNs.
                    properties:
                      arn:
                        description: ARN is the queue ARN (for AWS SQS).
                        type: string
                      deadLetterQueueUrl:
                        description: DeadLetterQueueURL is the DLQ URL if enabled.
                        type: string
                      url:
                        description: URL is the queue URL for sending messages.
                        type: string
                    type: object
                  storage:
                    description: Storage contains bucket information.
                    properties:
                      bucketName:
                        description: BucketName is the bucket name.
                        type: string
                      region:
                        description: Region is the bucket region.
                        type: string
                    type: object
                type: object
              observedGeneration:
                description: |-
                  ObservedGeneration is the spec generation last processed.
                  If this doesn't match metadata.generation, controller is still processing.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level summary of application state.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Failed
                - Deleting
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
